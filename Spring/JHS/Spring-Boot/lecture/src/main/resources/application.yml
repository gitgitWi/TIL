spring:
    profiles:
    active: local
    mvc:
    view:
        prefix: /WEB-INF/jsp/ 
        suffix: .jsp
    datasource:
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    url: jdbc:log4jdbc:mysql://127.0.0.1:3306/a5?autoReconnect=true&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC # DB명 치환
    username: root # DB아이디 치환
    password:

mybatis:
  type-aliases-package: io.jhs.lecture.dto # dto의 풀 패키지 명 입력